python3 -m venv myenv  # Create virtual environment named 'myenv' (can be replaced with any other name)

source myenv/bin/activate  # Activate the virtual environment (for linux) on windows we use "myenv\Scripts\activate"

pip install flask


