python3 -m venv myenv  # Create virtual environment named 'myenv' (can be replaced with any other name)

myenv\Scripts\activate  # Activate the virtual environment (for windows) on linux we use "source myenv/bin/activate"

pip install -r requirements.txt

